# The contact form posts to Formspree, whose endpoint is public by design and
# lives in the component itself (src/components/ContactForm.tsx). There is no
# key to set and nothing to configure for the form to work.
#
# This file is kept for future secrets. It is intentionally empty.
